version: "2"
sql:
  - engine: "postgresql"
    schema: "db/migrations"
    queries: "db"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_json_tags: true
        emit_db_tags: true
